import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ripple effect", description: "Explore the potential impact of living kidney donation." };

export default function RippleLayout({ children }: { children: React.ReactNode }) { return children; }
