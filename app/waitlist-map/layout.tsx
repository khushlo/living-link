import type { Metadata } from "next";

export const metadata: Metadata = { title: "Waitlist map", description: "Explore kidney waitlist data by state." };

export default function WaitlistMapLayout({ children }: { children: React.ReactNode }) { return children; }
