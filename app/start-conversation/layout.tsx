import type { Metadata } from "next";

export const metadata: Metadata = { title: "Practice conversation", description: "Practice talking about living kidney donation." };

export default function StartConversationLayout({ children }: { children: React.ReactNode }) { return children; }
